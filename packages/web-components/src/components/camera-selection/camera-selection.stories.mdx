import {Markdown, Meta, Story} from '@storybook/blocks';
import Props from './readme.md?raw'

<Meta title="Components/CameraSelection"/>

<Markdown>{Props}</Markdown>

export const CameraSelection = (args) => `<camera-selection cameras='${JSON.stringify([{label: 'Camera 1', deviceId: '3l1jdaqwe'}, {label: 'Camera 2', deviceId: '3l1jdaeqw'}])}'></camera-selection>`;

## CameraSelection

<Story name="CameraSelection">
  {CameraSelection.bind({})}
</Story>
