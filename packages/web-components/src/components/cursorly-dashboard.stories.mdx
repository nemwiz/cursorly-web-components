import {Meta, Story} from '@storybook/blocks';
import '../../dist/web-components/web-components.esm';

<Meta title="Dashboard"/>

export const CursorlyDashboard = (args) => `<div>
<script>window.addEventListener('cameraChanged', (event) => {window.cameraId = event.detail})</script>
<settings-component></settings-component>
<gesture-detector camera-id="${window.cameraId}"></gesture-detector>
</div>`;

<Story name="Cursorly dashboard">
  {CursorlyDashboard.bind({})}
</Story>
