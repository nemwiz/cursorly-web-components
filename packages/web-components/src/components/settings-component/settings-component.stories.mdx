import {Markdown, Meta, Story} from '@storybook/blocks';
import Props from './readme.md?raw'

<Meta title="Components/Settings"/>

<Markdown>{Props}</Markdown>

export const SettingsComponent = (args) => `<settings-component></settings-component>`;

## Settings

<Story name="Settings">
  {SettingsComponent.bind({})}
</Story>
