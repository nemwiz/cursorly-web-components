import {Markdown, Meta, Story} from '@storybook/blocks';
import Props from './readme.md?raw'

<Meta title="Components/Settings"/>

<Markdown>{Props}</Markdown>

export const SettingsComponent = (args) => `<settings-component screens='${JSON.stringify([{name: 'Display 1', screenId: 6786}, {name: 'Display 2', screenId: 4621}])}'></settings-component>`;

## Settings

<Story name="Settings">
  {SettingsComponent.bind({})}
</Story>
