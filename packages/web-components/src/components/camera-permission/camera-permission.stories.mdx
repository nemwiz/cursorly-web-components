import {Markdown, Meta, Story} from '@storybook/blocks';
import Props from './readme.md?raw'

<Meta title="Components/CameraPermission"/>

<Markdown>{Props}</Markdown>

export const CameraPermissionComponent = (args) => `
<section class="section">
  <div class="hero fullscreen">
    <div class="hero-body">
      <div class="content w-80p content-small-screen">
        <camera-permission></camera-permission>
      </div>
    </div>
  </div>
</section>
`;

## CameraPermission

<Story name="CameraPermission">
  {CameraPermissionComponent.bind({})}
</Story>
