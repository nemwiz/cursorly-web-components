import {Markdown, Meta, Story} from '@storybook/blocks';
import Props from './readme.md?raw'

<Meta title="Components/ScreenSelection"/>

<Markdown>{Props}</Markdown>

export const ScreenSelectionComponent = (args) => `<screen-selection screens='${JSON.stringify([{name: 'Display 1', screenId: 6786}, {name: 'Display 2', screenId: 4621}])}'></screen-selection>`;

## ScreenSelection

<Story name="ScreenSelection">
  {ScreenSelectionComponent.bind({})}
</Story>
