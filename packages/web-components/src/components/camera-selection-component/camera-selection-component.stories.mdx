import {Meta, Source, Story} from '@storybook/addon-docs';
import Props from './readme.md'

<Meta title="Components/CameraSelection"/>

<Props/>

export const CameraSelectionComponent = (args) => `<camera-selection-component></camera-selection-component>`;

## CameraSelection

<Story name="CameraSelection">
  {CameraSelectionComponent.bind({})}
</Story>
